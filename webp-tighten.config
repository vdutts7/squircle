# git-webp-tighten default sliding window (used when you run ./bin/git-webp-tighten.sh with no args)
# 1 = latest only (one copy of webp/ in history)
# 5 or 10 = keep last 5 or 10 commits' webp/ as-is; older commits get the Nth-from-tip's webp/
WINDOW=5
